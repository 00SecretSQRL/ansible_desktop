---
- hosts: localhost
  connection: local
  become: true


  tasks:
  - name: install stuff
    package:
      name: 
        - htop
        - git
        - tmux
        - zsh
        - docker.io
        - docker-compose
        - python3-pip
        - python3-venv
        - python3-dev